---
session_id: 2026-07-04-003
project: viralscan
branch: claude/multimap-memory-and-showcase
started: 2026-07-04T22:22:15+0200
ended: in-progress (hook fired early at 22:36; session continued)
files_changed: 6+
---

## Session Log

### 22:22 — Session started
- Branch: `claude/multimap-memory-and-showcase`
- Resuming from: 2026-07-04-002-viralscan.md

### 22:31 — Profiling pivot: killed full-data run, launched fast_profile.py
**Decision**: Killed the long-running profile_multimap.py (which was timing all 4 methods
on the full 103M-row BUS file with the OLD itertuples code). Rationale:
1. The 'equal' anchor (19,964s) is sufficient to prove vectorization is the bottleneck.
2. Non-EM methods share ONE pass — measuring them twice is pure waste.
3. Two perf refactors landed AFTER the first run: b7e9635 (zip loop, ~15% faster) and
   2c2e6f0 (vectorised em_gene_abundances). The old measurements are stale.
4. Phase 4 (subprocess RSS) had a broken timeout (7200s < 19964s actual) → all None.

**Action**: Created fast_profile.py to profile the CURRENT code:
- cProfile on 1M-row subsample (equal + em methods)
- Wall-time on 5M-row subsample (all 4 methods), extrapolated to full 103M rows
- EM convergence (instrumented vectorised loop on 5M subsample)
- All outputs written immediately as they land

**Status**: fast_profile.py running as nohup, PID 518744 (PPID=1, detached).
Phase 0 complete (confirmed: 103M records, 848k cells, 43k genes, 4984 MB RSS).
Phase 3 cProfile in progress.

### 22:36 — Partial session snapshot (hook fired mid-session; session continues)
- Modified so far: ANALYSIS_MANIFEST.md, fast_profile.py, profile_multimap.py
- fast_profile.py RUNNING (cProfile equal complete, EM cProfile in progress)

### 22:49 — CRITICAL FINDING: _matrix_value dominates (87.9% of build time)
cprofile_equal.txt (1M rows, 472.2s with cProfile overhead):
- `<listcomp>` line 292: cumtime=415.0s (87.9% of total)
- `_matrix_value` cumtime: 408.2s (86.5%)
- `scipy.sparse.__getitem__`: 366.1s; `_validate_indices`: 177.2s
- 5,873,997 individual scalar CSR lookups on 1M rows (avg ~7.2 genes per multi-EC record)
- Each lookup: __getitem__ → _validate_indices → isintlike (×4) → _get_intXint → get_csr_submatrix

**Performance bug flagged for report** (NO src/ changes per task constraint):
multimapping.py line 292: `_matrix_value(original_counts, cell_idx, gid)` called N times per
record (N = n_genes_in_ec). Fix: batch fetch `original_counts[cell_idx, genes_in_ec]` before
the gene loop → eliminates 5+ scipy dispatch layers per gene → expected ~8× speedup.

### 23:00 — EM cProfile complete (486.3s); hypothesis (b) REFUTED
cprofile_em.txt (1M rows, 485.4s with cProfile overhead):
- em_gene_abundances ABSENT from top-40 — vectorised (commit 2c2e6f0), below noise floor
- EM extra cost vs equal: 9.2s (= build_multimap_layers tottime delta: 36.2s − 27.0s)
- em_records allocation loop = sole EM overhead (2.0% of equal total)
- _matrix_value still 83.2% of EM total — bottleneck identical for all methods

Hypothesis (b) result: REFUTED (for current vectorised code)
- Pre-2c2e6f0: em_gene_abundances dominated EM extra cost
- Post-2c2e6f0: em_gene_abundances is free; em_records loop is the residual EM overhead (trivial)

interpret_cprofile.py updated to handle vectorised case and document both hypotheses correctly.
Phase 2 wall-time (5M rows, 4 methods) now running. Estimated completion: ~40-50 min.

### Files Modified (full session)
- `analysis/ANALYSIS_MANIFEST.md`
- `analysis/multimap_profiling/scripts/fast_profile.py`
- `analysis/multimap_profiling/scripts/interpret_cprofile.py`
- `analysis/multimap_profiling/scripts/profile_multimap.py`
- `.living/INDEX.md`
- `.living/log/2026-07-04-003-viralscan.md`
